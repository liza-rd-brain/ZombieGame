function parse(gamelist: GameList, index...): ManAndHealthItem {
    const cell = gamelist.get(index);
    if (cell .... ==== kshahfsjks) {
        return cell as ManAndHealthItem;
    } else {
        throw 'No ManAndHealthItem at index ${index}'
    }
}

const item = parse(gl, index);

// item === ManAndHealthItem